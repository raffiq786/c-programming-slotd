#include <stdio.h>

int main()
{
    char str;
    int i, sum = 0;

    printf("Enter the string: ");
    fgets(str, 100, stdin);

    for(i = 0; str[i] != '\0'; i++)
    {
        sum += (int)str[i];
    }

    printf("Sum of all characters: %d", sum);

    return 0;
}
